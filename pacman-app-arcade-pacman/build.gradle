plugins {
    id "java"
    id "org.openjfx.javafxplugin" version "0.1.0"
    id "org.beryx.jlink" version "3.1.1"
}

dependencies {
    implementation project(":pacman-core")
    implementation project(":pacman-ui-lib")
    implementation project(":pacman-ui")
    implementation libs.bundles.tinylog
}

javafx {
    modules = ["javafx.base", "javafx.graphics", "javafx.controls", "javafx.media"]
}

application {
    mainModule = "arcade.pacman"
    mainClass = "de.amr.pacmanfx.arcade.app.Main"
}

jlink {
    launcher {
        name = "Arcade Pac-Man"
    }
    jpackage {
        if (os.isWindows()) {
            installerType = "msi"
        }
        else if (os.isLinux()) {
            installerType = "deb"
        }
        else if (os.isMacOsX()) {
            installerType = "dmg"
        }
    }
}
