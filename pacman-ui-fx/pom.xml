<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>de.amr.games.pacman</groupId>
	<artifactId>pacman-ui-fx</artifactId>
	<version>1.0</version>

	<name>Pac-Man / Ms. Pac-Man UI in JavaFX</name>
	<description><![CDATA[
This is a JavaFX UI for the Pac-Man and Ms. Pac-Man games.

The game model and logic are implemented using the Model-View-Controller architecture, see https://github.com/armin-reichert/pacman-basic.

There are two executables built:
1. An executable jar file (using Maven shade)
2. A zip file containing the Java/JavaFX runtime and the game
  ]]>
	</description>
	<url>https://github.com/armin-reichert/pacman-javafx</url>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<classifier>win</classifier>
		<mainClass>de.amr.games.pacman.ui.fx.app.Main</mainClass>
		<version.java>18</version.java>
		<version.javafx>18.0.2</version.javafx>
		<version.mvn.compiler>3.10.1</version.mvn.compiler>
		<version.mvn.jar>3.2.0</version.mvn.jar>
		<version.mvn.javafx>0.0.8</version.mvn.javafx>
		<version.mvn.shade>3.4.1</version.mvn.shade>
		<version.tinylog>2.6.1</version.tinylog>
	</properties>

	<dependencies>

		<dependency>
			<groupId>de.amr.games.pacman</groupId>
			<artifactId>pacman-core</artifactId>
			<version>1.0</version>
		</dependency>

		<dependency>
			<classifier>${classifier}</classifier>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-controls</artifactId>
			<version>${version.javafx}</version>
		</dependency>

		<dependency>
			<classifier>${classifier}</classifier>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-media</artifactId>
			<version>${version.javafx}</version>
		</dependency>

		<dependency>
			<groupId>org.tinylog</groupId>
			<artifactId>tinylog-api</artifactId>
			<version>${version.tinylog}</version>
		</dependency>

		<dependency>
			<groupId>org.tinylog</groupId>
			<artifactId>tinylog-impl</artifactId>
			<version>${version.tinylog}</version>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${version.mvn.compiler}</version>
				<configuration>
					<release>${version.java}</release>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>${version.mvn.jar}</version>
				<configuration>
					<archive>
						<manifestEntries>
							<!-- this was added for log4j, not sure if still needed -->
							<Multi-Release>true</Multi-Release>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.openjfx</groupId>
				<artifactId>javafx-maven-plugin</artifactId>
				<version>${version.mvn.javafx}</version>
				<configuration>
					<mainClass>${mainClass}</mainClass>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>${version.mvn.shade}</version>
				<executions>
					<execution>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<shadedArtifactAttached>true</shadedArtifactAttached>
							<transformers>
								<transformer
									implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<mainClass>${mainClass}</mainClass>
								</transformer>
							</transformers>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>